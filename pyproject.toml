[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rs90-backtester"
version = "0.1.0"
description = "Point-in-time RS90 daily backtester for breakout and RSI2 setups"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
